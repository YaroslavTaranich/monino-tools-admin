name: Pipeline
run-name: ${{ github.actor }} is running Pipeline 🚀
on:
  push:
    branches:
      - main
jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🖥️ Runing build"
      - run: npm run build
